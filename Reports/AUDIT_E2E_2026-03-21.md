# Audit E2E — Scrap-module

**Data:** 21.03.2026, 11:38:06
**Cale proiect:** `C:/Projects/Scrap-module`
**URL testat:** https://nixjflndtpzzygewxpuf.supabase.co
**Generat de:** Master E2E Audit System (NGTWG)

---

## 1. Static Overview

| Proprietate | Valoare |
|------------|---------|
| Versiune | Python project |
| Tech stack | next, react, react-dom, @anthropic-ai/sdk, @supabase/supabase-js, @types/react, @types/react-dom, eslint-config-next |
| Framework Python | NU |
| Knowledge files | 1 fișiere |
| URL detectat | https://nixjflndtpzzygewxpuf.supabase.co |

## 2. README / Descriere proiect

_Lipsă README.md_

## 3. Strategie & Roadmap (din Knowledge)

### README.md
# Knowledge Base

Add your project documentation, specifications, and reference files here.

## Usage

Place files in this folder that Claude should reference during development:
- API specifications (.json, .yaml)
- Requirements documents (.md, .txt)
- Database schemas (.sql)
- Integration guides (.md)
- Any other reference material

## Supported Formats
- Markdown (.md)
- Text (.txt)
- JSON (.json)
- YAML (.yaml, .yml)
- SQL (.sql)
- Python (.py)
- TypeScript/JavaScript (.ts, .js)

## Integration with Claude

Files in this folder are automatically referenced in CLAUDE.md.
When you add files
_(trunchiat)_

## 4. Analiză Statică de Cod

### 4.1 Structura Folderelor

```
C:/Projects/Scrap-module/
├── src/
│   ├── app/
│   │   ├── page.tsx              — UI principal (client component, single page)
│   │   ├── layout.tsx            — Layout Next.js (root)
│   │   ├── globals.css           — Stiluri globale TailwindCSS
│   │   └── api/
│   │       ├── scrape/
│   │       │   └── route.ts      — POST endpoint: scrape URL via Claude AI
│   │       └── generate/
│   │           └── route.ts      — POST endpoint: generare fișier descărcabil
│   ├── hooks/
│   │   └── useScraper.ts         — Custom hook (definit dar neutilizat în page.tsx)
│   └── lib/
│       ├── scraper.ts            — Logica de scraping cu Anthropic SDK
│       └── formatter.ts          — Conversie date → JSON/CSV/XML
├── knowledge/
│   └── README.md                 — Template gol (fără specificații reale)
├── backup.js                     — Sistem backup status proiect
├── status-update.js              — CLI pentru vizualizare backup status
├── backup-status.json            — Snapshot stare proiect
├── START_SCRAP.bat               — Script pornire Windows
├── package.json                  — Next.js 16, Anthropic SDK, Supabase, OpenAI
├── next.config.js
├── tsconfig.json
├── .env.local                    — ANTHROPIC_API_KEY, OPENAI_API_KEY, Supabase credentials
└── Reports/
```

**Rolul fiecărui folder:**
- `src/app/` — Aplicația Next.js App Router: UI + 2 API routes
- `src/hooks/` — Custom hook `useScraper` (definit dar neutilizat)
- `src/lib/` — Core logic: scraping cu Claude AI și formatare output
- `knowledge/` — Template gol, fără documentație reală
- Fișiere utilitare în root: backup, status, script pornire Windows

### 4.2 Funcționalități Implementate

| Funcționalitate | Status |
|----------------|--------|
| Input URL website | Implementat 100% |
| Selectare format output (JSON/CSV/XML) | Implementat 100% |
| Upload fișier template opțional (.json, .csv, .xml, .txt) | Implementat 100% |
| Scraping URL via Claude AI (claude-3-5-sonnet) | Implementat 100% |
| Extragere date structurate din HTML (max 5 items) | Implementat 100% |
| Preview date extrase (max 100 chars per câmp) | Implementat 100% |
| Confirmare înainte de generare fișier | Implementat 100% |
| Generare fișier JSON / CSV / XML descărcabil | Implementat 100% |
| Download automat în browser | Implementat 100% |
| Validare URL format | Implementat 100% |
| Error handling cu mesaje utilizator | Implementat 100% |
| Formatare CSV cu escape corect pentru virgule/ghilimele | Implementat 100% |
| Formatare XML cu escape entități speciale | Implementat 100% |
| Supabase integration (DB) | Neimplementat (pachet instalat, niciun apel) |
| OpenAI integration | Neimplementat (pachet instalat, niciun apel) |
| Autentificare utilizatori | Neimplementat |
| Istoric scraping-uri | Neimplementat |
| useScraper hook utilizat în UI | Neimplementat (hook definit, dar ignorat) |

### 4.3 Rute / Endpoint-uri

| Rută | Tip | Status | Descriere |
|------|-----|--------|-----------|
| `/` | GET (pagina) | Implementat | UI principal — single page app |
| `/api/scrape` | POST | Implementat | Scrape URL și returnează preview (FormData) |
| `/api/generate` | POST | Implementat | Generează și returnează fișier descărcabil (JSON body) |

**Payload `/api/scrape` (FormData):**
- `url` (string, required)
- `format` (string: json/csv/xml)
- `file` (File, optional — template)

**Response `/api/scrape`:**
```json
{
  "success": true,
  "preview": [...items],
  "count": 5,
  "fileInfo": { "name": "...", "size": ..., "type": "..." } | null,
  "message": "Found X items..."
}
```

**Payload `/api/generate` (JSON):**
```json
{
  "items": [...],
  "format": "json|csv|xml",
  "filename": "optional",
  "templateFile": { "name": "...", "content": "..." }
}
```
**Response `/api/generate`:** Blob descărcabil cu Content-Disposition header

### 4.4 Componente UI și Butoane

| Element UI | Locație | Funcție |
|-----------|---------|---------|
| Input "Website URL" | page.tsx | URL de scraped |
| Select "Output Format" (JSON/CSV/XML) | page.tsx | Format fișier output |
| Input file "Upload Template File" | page.tsx | Fișier template opțional |
| Buton "Scrape Website" | page.tsx | Declanșează scraping |
| Afișaj preview items (scrollabil) | page.tsx | Vizualizare date extrase |
| Buton "Cancel" | page.tsx (preview) | Resetează și revine la formular |
| Buton "Confirm & Continue" | page.tsx (preview) | Confirmă și merge la pasul filename |
| Input "Filename" (opțional) | page.tsx (confirmed) | Nume fișier de descărcat |
| Buton "Back" | page.tsx (confirmed) | Revine la preview |
| Buton "Download JSON/CSV/XML" | page.tsx (confirmed) | Generează și descarcă fișierul |
| Mesaj eroare (div roșu) | page.tsx | Afișare erori din API |

**Flow utilizator:**
1. Completează URL + format + (opțional) template → "Scrape Website"
2. Vizualizează preview → "Confirm & Continue" sau "Cancel"
3. Completează (opțional) filename → "Download" sau "Back"

### 4.5 Calitatea Codului

**Puncte pozitive:**
- Separare clară: UI (page.tsx) / logică server (scraper.ts, formatter.ts) / API routes
- Formatter bine structurat cu funcții separate per format
- Escape corect pentru CSV (ghilimele, virgule) și XML (entități speciale)
- Validare URL în ambele endpoint-uri (scrape + generate)
- Error handling consistent în API routes cu status codes corecte
- TypeScript cu tipizare clară

**Probleme identificate:**
- **`useScraper.ts` — dead code complet** — hook-ul este definit în `src/hooks/` dar `page.tsx` implementează logica direct cu `useState`. Hook-ul nu este importat nicăieri.
- **Template file ignorat** — fișierul template upload-at ajunge la `/api/generate`, dar logica din `generate/route.ts` îl ignoră complet pentru JSON (comentariul `// For JSON, add as metadata comment` e urmat de `content = content` — fără modificare efectivă).
- **Limit de 5 items hardcodat** — `scraper.ts` returnează max 5 items indiferent de dimensiunea paginii. Nu există configurare pentru utilizator.
- **HTML trimis la Claude trunchiat** — `html.substring(0, 8000)` poate pierde structura importantă a paginii.
- **Fără paginare/scroll** în preview pentru mai mult de 5 items (redundant cu limita de 5).
- **Dependențe neutilizate** — `@supabase/supabase-js` și `openai` sunt instalate dar niciun apel nu există în cod.
- **STRATEGY.md gol** — conține template placeholder (`[Define the project vision]`), fără strategie reală.
- **Knowledge base gol** — `knowledge/README.md` este template, fără specificații de proiect.

### 4.6 Securitate

| Aspect | Status | Detalii |
|--------|--------|---------|
| ANTHROPIC_API_KEY expus | RISC CRITIC | `.env.local` conține cheia reală completă: `sk-ant-api03-mJU9...` |
| OPENAI_API_KEY expus | RISC CRITIC | `.env.local` conține cheia reală completă: `sk-proj-uM13...` |
| NEXT_PUBLIC_SUPABASE_ANON_KEY expus | Risc mediu | Cheia este `NEXT_PUBLIC_` deci intenționat publică, dar JWT complet vizibil |
| NEXT_PUBLIC_SUPABASE_URL expus | OK intenționat | URL Supabase public prin design |
| Server-side API calls | OK | Anthropic SDK folosit în `scraper.ts` (server-side), nu expus în client |
| Input sanitization | Parțial | URL validat cu `new URL()`, dar conținutul HTML de la URL extern este trimis direct la Claude |
| SSRF (Server-Side Request Forgery) | Risc | `/api/scrape` poate fi folosit să facă fetch la orice URL, inclusiv rețele interne |
| Fără rate limiting | Risc | Orice client poate face request-uri nelimitate la `/api/scrape` |
| Fără autentificare | Risc | Endpoint-urile sunt publice, oricine poate declanșa apeluri Claude costisitoare |

### 4.7 Starea API-urilor externe

| Serviciu | Status | Detalii |
|---------|--------|---------|
| Anthropic API (claude-3-5-sonnet-20241022) | Configurat și utilizat | Cheie prezentă în `.env.local`, folosit în `scraper.ts` |
| OpenAI API | Instalat, neutilizat | Pachet `openai` în dependencies, zero apeluri în cod |
| Supabase | Configurat, neutilizat | URL + anon key prezente, zero apeluri în cod |

### 4.8 Probleme Critice Identificate

1. **Chei API reale expuse în `.env.local`** — `ANTHROPIC_API_KEY` și `OPENAI_API_KEY` conțin chei reale, cu risc de expunere dacă `.env.local` ajunge în repository (verificați `.gitignore`).
2. **SSRF vulnerability** — Serverul face fetch la orice URL furnizat de utilizator fără whitelist sau validare de domeniu.
3. **Template file complet ignorat** — Feature-ul de upload template este parțial implementat (acceptat în scrape, ignorat în generate pentru JSON), creând o experiență utilizator înșelătoare.
4. **`useScraper` hook — dead code** — Hook definit și nevândut nicăieri, creând confuzie arhitecturală.
5. **Dependențe neutilizate cu cost** — `openai` și `@supabase/supabase-js` adaugă ~2MB la bundle fără niciun beneficiu funcțional.

## 5. Browser Testing E2E

```
error: unknown option '--accessibility'

```

## 6. Concluzii & Recomandări

### Scor General: 6/10

### Ce funcționează 100%
- Flow complet de scraping: URL → Claude AI → preview → confirmare → download
- Toate cele 3 formate de output (JSON, CSV, XML) generate corect
- Validare URL pe front-end și back-end
- Formatare CSV cu escape corect pentru cazuri speciale
- Formatare XML cu entități speciale escape corect
- Error handling cu mesaje utilizator clare
- Upload fișier template (acceptat și transmis, chiar dacă ignorat în generate)

### Ce este parțial implementat
- **Template file** — acceptat în flow, transmis prin API, dar ignorat în logica de generare (nu modifică outputul)
- **Supabase** — configurat în `.env.local`, pachet instalat, zero integrare în cod
- **`useScraper` hook** — implementat complet dar niciodată utilizat (logica duplicată în `page.tsx`)

### Ce lipsește complet față de roadmap
- Strategie de produs reală (STRATEGY.md conține placeholder-e)
- Autentificare utilizatori (endpoint-urile sunt complet publice)
- Istoric scraping-uri (Supabase ar fi perfect pentru aceasta)
- Rate limiting / protecție abuz
- Configurare utilizator pentru numărul de items extrase (fix 5 în prezent)
- README.md utilizator

### Top 5 Probleme Critice

| # | Problemă | Prioritate |
|---|---------|-----------|
| 1 | Chei API reale (`ANTHROPIC_API_KEY`, `OPENAI_API_KEY`) prezente în `.env.local` — risc de leak dacă fișierul nu este în `.gitignore` | CRITIC |
| 2 | SSRF — serverul face fetch la orice URL fără restricții (posibil acces la rețele interne) | CRITIC |
| 3 | Endpoint-uri `/api/scrape` și `/api/generate` fără autentificare sau rate limiting — cost necontrolat Claude API | RIDICAT |
| 4 | Feature "template file" inexistent în practică — UX înșelător | MEDIU |
| 5 | `useScraper` hook — dead code care creează confuzie arhitecturală | SCĂZUT |

### Recomandări Next Steps

1. **Imediat:** Verificați că `.env.local` este în `.gitignore` și rotați cheile API dacă au fost commituite vreodată
2. **Imediat:** Adăugați validare/whitelist pentru domenii permise la scraping (anti-SSRF)
3. **Short-term:** Implementați template file logic în `generate/route.ts` — mapați structura template la datele scraped
4. **Short-term:** Eliminați sau utilizați `useScraper` hook — refactorizați `page.tsx` să îl folosească
5. **Medium-term:** Implementați Supabase pentru istoric scraping-uri și opțional autentificare utilizatori
6. **Medium-term:** Adăugați rate limiting (ex: max 10 request-uri/oră per IP)
7. **Medium-term:** Completați STRATEGY.md cu viziunea reală a produsului

---
*Raport generat automat · Master E2E Audit System · 2026-03-21*
